#!/usr/bin/python3
def update_dictionary(a_dictionary, key, value):
    new_dict = {}
    for key, value in a_dictionary.items():
        if key == key:
            new_dict[key] = value
        else:
            new_dict[key] = value
    return new_dict
